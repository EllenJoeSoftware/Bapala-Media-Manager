<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Declare that this app works on TV (leanback) as well as phones.
         required=false keeps it installable on phones too. -->
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <!-- TV remotes have no touchscreen -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/appicon"
        android:roundIcon="@mipmap/appicon_round"
        android:supportsRtl="true"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:banner="@mipmap/appicon">

        <!-- LEANBACK_LAUNCHER makes the app appear in the Android TV home screen launcher -->
        <activity android:name="com.bapala.app.MainActivity"
                  android:exported="true">
            <!-- Phone/tablet launcher — shows one icon in the regular app drawer -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- TV launcher — shows the app on the Android TV home screen.
                 Kept in a separate intent-filter so phone launchers don't
                 pick it up as a second icon. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
